export interface LayoutConfig {
  showHeader?: boolean;
  showFooter?: boolean;
  showHeroCta?: boolean;
  centered?: boolean;
  fullHeight?: boolean;
  backgroundImage?: string;
  backgroundImageSize?: "big" | "small";
}
